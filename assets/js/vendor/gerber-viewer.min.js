// Placeholder for tracespace/gerber-viewer library
// Please replace this file with the actual gerber-viewer.min.js from https://github.com/tracespace/gerber-viewer/releases
console.log('GerberNAMA: Placeholder for gerber-viewer.min.js loaded. Real library needed for viewer functionality.');

// Basic function definition to prevent "gerberViewer is not a function" if called by other scripts.
if (typeof gerberViewer === 'undefined') {
    function gerberViewer(target, options) {
        console.warn('GerberNAMA: gerberViewer() called, but this is a placeholder library.');
        if (target) {
            target.innerHTML = '<p style="color:red; text-align:center; padding-top: 20px;">Gerber Viewer REAL LIBRARY NOT LOADED. This is a placeholder.</p>';
        }
        // Return a mock object with a loadLayers function to prevent further errors.
        return {
            loadLayers: function(urls) {
                console.warn('GerberNAMA: viewer.loadLayers() called on placeholder library with URLs:', urls);
                return Promise.reject(new Error('Cannot load layers: Real gerber-viewer library not installed.'));
            }
        };
    }
}
